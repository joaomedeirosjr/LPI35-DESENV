import AthleteAnalyticsPage from "../athlete/AthleteAnalyticsPage"

export default function AdminAthleteAnalyticsPage() {
  // embedded = true => encaixa dentro do AdminLayout (sem layout full-screen)
  return <AthleteAnalyticsPage embedded />
}